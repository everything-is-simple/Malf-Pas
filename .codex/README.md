# Malf-Pas Codex boundary

This directory may contain repo-local Codex skills or plugin notes for Malf-Pas.

Rules:

```text
no personal secrets
no H:/Asteria hard-coded workflow paths
no copied virtualenv
no business runtime migration
```

Local user configuration such as `.codex/config.toml` remains ignored.
