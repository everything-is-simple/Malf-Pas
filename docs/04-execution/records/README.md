# 执行记录目录

后续每张卡的四件套统一放在本目录的子目录中：

```text
records/<route_or_module>/<run_id>.card.md
records/<route_or_module>/<run_id>.evidence-index.md
records/<route_or_module>/<run_id>.record.md
records/<route_or_module>/<run_id>.conclusion.md
```

首轮仓库初始化阶段只建立结构，不预先伪造执行记录。

